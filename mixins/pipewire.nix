{ config, lib, ...}:
with lib;
{
  config = mkDefault {
    services.pipewire = {
      enable = true;
      alsa.enable = true;
      alsa.support32Bit = true;
      pulse.enable = true;
    };
  };
}
